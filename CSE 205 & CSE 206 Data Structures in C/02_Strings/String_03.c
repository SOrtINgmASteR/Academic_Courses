#include<stdio.h>
#include<string.h>
int main()
{
    char names[100][1000] = {"Priom", "Sharif", "Ovi", "Tausif", "Mesbah"};
    puts(names[0]);
    puts(names[1]);
    puts(names[2]);
    puts(names[3]);
    puts(names[4]);
    return 0;
}