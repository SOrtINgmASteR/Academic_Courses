int main() {
  int x, y;
  float f;

  x = 10;
  y = x + 20 * 3;

  print(y);
  print("hello TinyC");

  if (y >= 50) {
    int x; // shadowing is allowed in inner scope
    x = 1;
    print(x);
  } else {
    y = y - 1;
  }

  while (x < 15) {
    x = x + 1;
  }

  return y;
}
